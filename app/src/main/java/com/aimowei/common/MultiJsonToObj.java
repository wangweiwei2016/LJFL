package com.aimowei.common;

public class MultiJsonToObj {
	public void parseUserFromJson(String JsonData) {

		/*
		 * Type listType =new TypeToken<LinkedList<Accout>>(){}.getType(); Gson
		 * gson=new Gson(); LinkedList<Accout>
		 * accounts=gson.fromJson(JsonData,listType); for(Iterator
		 * iterator=accounts.iterator();iterator.hasNext();) { Accout
		 * account=(Accout)iterator.next(); }
		 */

	}

}
